# Release Notes 5.05.00

Rule-review wave 1 — resolver wiring + obsolete retirement.

## What changed
- Per-build part-context resolver wired into the entry path (START HERE + kickoff) with creative-review-first part discovery.
- Five mis-scoped foundational rules re-scoped so the resolver surfaces them correctly.
- `PIPE_BUBPIPE_VALIDATION_EXCEPTION_RULE` (3.01.01) retired as self-superseded by the 3.01.02 contextual-connector rule; references repointed.

## Deferred (with preservation constraints, see transfer summary)
- JSON ladder -> one staged protocol (preserve the XnZY/RIGHT_AT_UP/POST_RX90/LOCAL_Y_180 mapping verbatim).
- Precedence/authority docs -> one law (preserve every tier).
- Part-map library docs -> one rule (preserve SNAP_VALIDATED/GOLD tiers + non-snap fallback).
- Airlock 3 rules -> one recipe; geometry-derived-placement family -> one doctrine; creative learning merges; PART_FAMILY_RULES -> findings migration into the creative index.

## Gate
Run `python3 release/release_check.py` -> RELEASE GATE: PASS.
